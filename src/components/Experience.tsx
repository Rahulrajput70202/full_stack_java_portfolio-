import { motion } from 'framer-motion'
import { BriefcaseBusiness } from 'lucide-react'
import { experience } from '../data/portfolioData'
import { fadeInUp, staggerContainer, staggerItem, viewportOnce } from '../lib/animations'

export default function Experience() {
  return <section id="experience" className="border-t"><div className="section-container"><motion.div initial="hidden" whileInView="visible" viewport={viewportOnce} variants={fadeInUp}><p className="mb-3 font-mono text-sm accent-text">04 / experience</p><h2 className="section-title">Where I've Worked</h2></motion.div><div className="relative mt-12"><motion.div initial={{ height: 0 }} whileInView={{ height: '100%' }} viewport={viewportOnce} transition={{ duration: 1.2, ease: [0.16, 1, 0.3, 1] }} className="absolute bottom-0 left-3 top-0 w-px origin-top accent-bg opacity-50 sm:left-5" />{experience.map((entry) => <motion.div key={entry.company} initial="hidden" whileInView="visible" viewport={viewportOnce} variants={staggerContainer} className="relative pl-12 sm:pl-16"><div className="absolute left-0 top-1 flex h-7 w-7 items-center justify-center rounded-full border-4 border-[var(--bg)] accent-bg sm:left-2 sm:h-7 sm:w-7"><BriefcaseBusiness size={12} className="text-white" /></div><motion.div variants={staggerItem} className="max-w-3xl"><div className="flex flex-wrap items-center gap-x-3 gap-y-1"><h3 className="text-xl font-semibold">{entry.role}</h3><span className="rounded-full bg-[rgba(var(--accent-rgb),0.1)] px-2.5 py-1 text-xs font-medium accent-text">{entry.remote ? 'Remote' : entry.location}</span></div><p className="mt-1 font-medium accent-text">{entry.company}</p><p className="mt-2 text-sm text-muted">{entry.dates} · {entry.location}</p><ul className="mt-6 space-y-3">{entry.bullets.map((bullet) => <li key={bullet} className="flex gap-3 text-sm leading-relaxed text-muted sm:text-base"><span className="mt-2 h-1.5 w-1.5 shrink-0 rounded-full accent-bg" />{bullet}</li>)}</ul></motion.div></motion.div>)}</div></div></section>
}
